function sumar(x,y) {
    let resultado = x + y;
    console.log(resultado);
    return resultado;    
}

sumar(x, y);

function restar(x,y) {
    let resultado = x - y;
    console.log(resultado);
    return resultado;    
}
restar(x,y);

function dividir(x,y) {
    let resultado = x / y;
    console.log(resultado);
    return resultado;    
}

dividir(x,y);

function multiplicar(x,y) {
    let resultado = x * y;
    console.log(resultado);
    return resultado;    
}

multiplicar(x,y);
